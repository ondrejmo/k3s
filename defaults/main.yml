---

k3s:
  version: "1.22.5+k3s1"
  url: https://localhost:6443
  token: raspberry # /var/lib/rancher/k3s/server/node-token
k3s_s3: {}
  # host: localhost
  # access_key: minioadmin
  # secret_key: minioadmin
  # region: westeros
  # bucket: k3s
  # folder: etcd
k3s_registries: ""
k3s_labels: []
